{
  "name": "adaptive-detail-views-workspace",
  "private": true,
  "workspaces": [
    "packages/app"
  ],
  "scripts": {
    "dev": "npm run dev -w packages/app",
    "build": "npm run build -w packages/app",
    "build:widget": "npm run build:widget -w packages/app",
    "lint": "npm run lint -w packages/app",
    "test": "npm run test -w packages/app",
    "preview": "npm run preview -w packages/app"
  }
}
